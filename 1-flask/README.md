# Getting started

0. Create virtual environment `python3 -m venv env`
0. Activate virtual environment `source ./env/bin/activate`
0. Install requirements `python3 -m pip install -r requirements.txt`
0. Run the app by running `python3 src`
